3)	Generation of ODD number series 1, 3, 5, …..n 

#include<stdio.h>
int main()
{
	int n,i;
	printf("Enter a number :");
	scanf("%d",&n);
	printf("ODD number till %d :\n",n);
	for(i=1;i<=n;i=i+2)
	{
		printf("%d,",i);
	}
	return 0;
}
